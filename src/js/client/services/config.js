angular.module('www.tekuchi.converter').service('config', [
    function($location)                                                         {
        return {
            SCROLLBAR_WIDTH:     0
        };
    }
]);
